apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: database-pvc
    namespace: staging
spec:
    resources:
        requests:
            storage: 10Gi
    accessModes:
        - ReadWriteOnce
